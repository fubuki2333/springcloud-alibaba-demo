server:
  port: 9004
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.249.128:8848 #配置nacos地址
#        server-addr: 127.0.0.1:1111 #换成nginx1111端口，做集群
management:
  endpoints:
    web:
      exposure:
        include: "*"
